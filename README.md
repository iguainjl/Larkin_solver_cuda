# Nonlinear Larkin Equation Solver (CUDA)

CUDA C implementation of the nonlinear Larkin equation in 1D:

∂t h = ν ∂x ( (∂x h)^(2n-1) ) + η(x)

with quenched random disorder η(x).

## Build

```bash
make

